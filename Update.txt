2026-06-28 (57th Update)
- Resolved Git repository safety and tracking issues by removing untracked dependencies and improving ignore rules.
  1. Untracked node_modules: Removed accidentally tracked `node_modules` from Git index via `git rm -r --cached` while preserving the local files.
  2. Untracked test images: Removed Stable Diffusion test output images `test_sd_monster.png` and `test_sd_monster_transparent.png` from Git index.
  3. Optimized .gitignore: Added ignore patterns for Python runtime caches (`__pycache__`, `.py[cod]`, `*$py.class`), virtual environments (`.venv`, `venv`, `env`), and custom SD test images (`test_sd_monster*.png`).

2026-06-28 (56th Update)
- Established versioning system starting at V0.1.0 (currently aligned to V0.155 / 55th Update).
  1. UI implementation: Added gameVersion div under the mainMenu subtitle in index.html, populated dynamically via GAME_VERSION in main.ts.
  2. Rule enforcement: Sub-version increments on each subsequent update. V [major].[minor] will only increment if a /release command is explicitly given.
  3. Synced package.json version to 0.1.55.

2026-06-28 (55th Update)
- Implemented Dead Cells style modern pixel mix visual upgrades in the main game.
  1. Default zoom: Auto-zooms mapScale to 2.0 and centers vertical offset to -320 when starting BATTLE on 80x40 large maps, rendering 16px sprites at 32px size for clarity.
  2. Scroll zoom: Registered desktop mouse wheel event listener on canvas to pan/scale around cursor.
  3. Element trail bullets: Remade bullets into glowing element cores with multi-stage alpha fading trails tracing target direction.
  4. Base shadows: Added translucent base shadow ellipses underneath pixel enemies to remove float feeling.
  5. Sprite glowing outlines: Applied element-specific shadowBlur glows on pixel towers/enemies and a bright red glow on hit-flashing.
  6. Death shockwaves: Spawn expanding colored shockwave rings on monster deaths.

2026-06-28 (54th Update)
- Created showcase.html: Standalone visual quality showcase page to validate the visual ceiling of existing 16×16 pixel sprite matrices.
  1. Renders all sprites at 4× scale (sprites.ts-compatible drawPixelSprite logic ported to vanilla JS).
  2. Tower display: Three Lv1 towers (Fire/Water/Yang) with shadowBlur glow, breathing squash/stretch animation, orbiting element particles; one Lv2 Fire tower (16×24 isometric sprite) with level star indicator.
  3. Enemy display: Snake (2-frame walk) and Dragon Boss (2-frame wing flap) with sway animation; automatic hit-flash (white fill + red shadowBlur) + squash + element particle burst every 2.2s.
  4. Bullet effects: 5 element-typed bullets (fire/water/wood/metal/yin) with radialGradient cores, element-color shadowBlur halos, and 6-step pixel trail.
  5. Hit/explosion panel: Click-triggered or auto-demo particle burst (16 colored pixels + white core pixels) + ring shockwave with animated radius expansion.
  6. Full battle scene demo (960×300): 3 towers auto-target and fire at 3 moving enemies; complete VFX pipeline: bullet homing trail → hit particle burst → white flash + squash → death explosion (ring + scatter) → HP bar depletion → enemy auto-respawn.
  Purpose: Verify that the current sprite design quality is bounded by TILE_SIZE (16px display) not the matrix art detail, and establish baseline for deciding next steps (sprite redesign vs. effect layer upgrade vs. tile size increase).

2026-06-28 (53rd Update)
- Synced README.md and ROADMAP.md with latest development states.
  1. Updated README.md to describe pixel art visuals (Option D), coordinate viewport mapping, Audio Pool fallback system, and directional path overlay.
  2. Updated ROADMAP.md to document completed Phase 4.5 (Pixel Art Visuals) and Phase 4.6 (Adaptive Scale & Audio Pool).

2026-06-28 (52nd Update)
- Implemented Static Audio Pool & BGM Loading Fallback System.
  1. Resolved WebMediaPlayer instances leak: Replaced dynamic cloneNode audio creation in playSFX with a static HTML5 Audio pool cache (sfxAssets) capped at 10 active instances per SFX type. Handled automatic fallback recycling, completely resolving Chrome's "too many WebMediaPlayers" warning and improving runtime rendering performance.
  2. Implemented BGM error handling inside playNextBGM: Added error listener to HTMLAudioElement. When target music resource is missing or blocks loading, the player automatically stops and advances to the next track.
  3. Added BGM error count boundary protection: Automatically shuts down background audio loading if consecutive errors exceed BGM_PLAYLIST length, avoiding deadlocks or loop errors.

2026-06-28 (51st Update)
- Upgraded Battle Route Preview & Permanent Path Tiles Color Contrast with Directional Indicators.
  1. Redesigned path tile palettes in src/sprites.ts: Updated TILE_SCIFI_PATH_PALETTE and TILE_STARRY_PATH_PALETTE to map cyan highlight indexes (#06b6d4, #0891b2) to amber/orange colors (#f59e0b, #d97706), cleanly contrasting path tiles against blue obstacles.
  2. Implemented permanent subtle directional arrow guidelines: Added cachedFullPath variable in main.ts and drawing overlay logic in drawMap, rendering a 25% transparent golden guide line with white micro arrows flowing constantly in the monster's path direction.
  3. Refactored interactive route preview in main.ts: Redesigned drawRoutePreview to paint an amber glowing line with white direction arrows, both synced to Date.now() for seamless layout integration.

2026-06-28 (50th Update)
- Resolved Mouse Click & Touch Coordinate Mismatch under CSS Layout Scale.
  1. Implemented physical-to-logical viewport mapping on canvas interaction listeners: Multiplied Client position relative offsets by canvas resolution scaling factor (width/rect.width and height/rect.height) inside main.ts click and contextmenu events.
  2. Applied coordinate mapping adjustments to Map Editor: Corrected pointer coordinates calculations for editorCanvasEl mousedown and mousemove listeners to match grid tile bounds when scale is applied.
  3. Corrected pinch-to-zoom center point targeting: Added proportional coordinate translation to dual-finger center calculations in touchmove event.

2026-06-28 (49th Update)
- Implemented Dual-Axis Adaptive Sizing for Game Viewport & Fixed Test Level Bottom Panel Obstruction.
  1. Refactored resizeGameContainer in src/main.ts: Introduced dual-axis scaling (scaleX and scaleY matching viewport width and height dimensions) to prevent UI truncation on smaller display setups.
  2. Integrated dynamic layout base-height metrics: Configured a taller default baseHeight (830px) specifically for 'test_level' (due to extra #testControls options wrapping and extending grid bounds), reverting to 780px for normal stages.
  3. Added margin padding safeguards: Applied 12px padding window bounds buffers to scale computations to avoid extreme screen boundary collisions.

2026-06-28 (48th Update)
- Expanded Test Level permissions & added target-spawn debug controls.
  1. Opened build permissions for all basic and recipe-merged towers in Test Level: Configured buildTowerButtons to bypass isTowerUnlocked, and performMerge to bypass taiji_dao talent checks when currentMap is 'test_level'.
  2. Replaced immune target logic with full damage calculation in Test Level: Removed map checks in updatePhysics so spawned test monsters correctly receive damage, slow effects, and DOT ticks from towers.
  3. Implemented a dynamic target-spawn debugger UI: Added #testControls in index.html, containing a monster selection dropdown list and a Spawn button. Bound event listener in src/main.ts to call spawnTestEnemy, generating a designated monster at SPAWN_POINT instantly.

2026-06-28 (47th Update)
- Redesigned Level 1 Basic Towers with element-only minimalist icons.
  1. Completely removed the heavy gray brick bases/boxes from Level 1 towers in src/sprites.ts to eliminate unnecessary pixel noise and clutter.
  2. Recrafted 7 basic towers (fire, water, wood, earth, metal, yin, yang) into high-contrast, minimalist symbols representing their raw elements (e.g. pure flame for Fire, crystal drop for Water, clean leaf for Wood) to optimize battlefield scan-ability and pixel art clarity.
  3. Restructured F0 & F1 frame matrices for these elements to create subtle breathing and pulsing motions during idle state.

2026-06-28 (46th Update)
- Overhauled Level 3 recipe-based merged towers pixel visuals & reverted in-game gold upgrade system.
  1. Reverted all in-game gold tower upgrade modifications in src/main.ts, src/towers.ts, and index.html (removed star rating system, upgrade button, key bindings, range ring highlight, and upgrade refund formulas) to preserve code cleanliness.
  2. Hand-designed 6 unique 16x24 isometric 2.5D pixel art matrices with 2-frame breathing animations for Level 3 recipe-merged towers (wood_fire, fire_earth, earth_metal, metal_water, water_wood, yin_yang) in src/sprites.ts.
  3. Configured TOWER_SPRITE_MAP to support recipe-merged tower asset keys, and updated drawTowerSprite rendering logic to bypass recipe base mapping for these high-tier towers while correctly displaying their '★★★' level markings.

2026-06-28 (45th Update)
- Implemented In-Game Tower Upgrade & Star-Rating System.
  1. Modified Tower interface in src/main.ts: Added star rating (star, default 1, max 5) and spentUpgradeGold properties to track tower enhancement status.
  2. Implemented dynamic attribute calculation: Created applyTowerStarAttributes to compute enhanced damage (+20% per star), range (+0.2 grid per star), and fireRate (-4% cooldown frames per star) on target tower instance dynamically. Added upgrade logic to exclude pure walls ('earth') from upgrades.
  3. Structured upgrade cost, sell refunds, and merge refund formulas: Configured upgrade cost as `materialCost * 0.6 * currentStar`. Added 70% refund of spent upgrade gold upon selling towers. Enabled merge star inheritance (inherits higher star) and refunded 70% of spent upgrade gold of the consumed tower if its star was equal or lower, transferring upgrade progress losslessly.
  4. Implemented Selected Tower visual indicators: Added transparent range preview ring matching the element theme color, and a high-contrast selected border when clicking a tower. Added yellow retro '★n' star indicators rendered right above enhanced towers.
  5. Implemented interactive controls & key bindings: Integrated dynamic Update button in control HUD. Added [U] key to trigger upgrade and [S] key to trigger sell refunds.

2026-06-28 (44th Update)
- Implemented Zero-Filter Runtime Performance Optimization & Battle Diagnostics HUD.
  1. Baked outlines into offscreen sprite generation in prerenderSprite: Moved element-specific outlines and shadow offsets into the prerendering phase. Completely eliminated the expensive drop-shadow filters from drawTowerSprite and drawEnemySprite, reducing runtime GPU usage.
  2. Pre-generated hit-flash sprite cache: Created an offscreen '_flash' (white/red overlay) canvas for every sprite during prerendering. Removed the brightness/contrast filters on hit, replacing them with a fast texture blit of the pre-rendered white sprite.
  3. Implemented hardware-accelerated highres overlays: Applied source-atop composite blend overlays for highres hit flashing, bypassing CSS canvas filters.
  4. Created Battle Diagnostics HUD panel: Implemented realtime counters for FPS, Render Latency (ms), and Draw Calls in battle. Added an "Export Profile" button to print a formatted JSON report (Console Table) containing active counts and sprite cache lists.

2026-06-28 (43rd Update)
- Implemented Dual-Track Hand-drawn Monster Visual Overhaul (Option D Part 2).
  1. Re-designed 6 common monsters in Chibi Flat style: Extracted 16x16 chibi grid specs from the cat reference image (large head proportion, flat colors, 2-tone minimal shadow, strong outline). Applied this aesthetic to Snake, Fly, Salamander, Water Spirit, Golem, and Beetle, reducing visual clutter when multiple units gather.
  2. Re-designed Boss Dragon in Volumetric 16x24 style: Upgraded the boss dragon model to a detailed 16x24 isometric flying asset. Utilized 45-degree multi-tone volumetric shading and selective outlines (golden flares, deep crimson shades) to give the boss extreme 3D depth and presence.

2026-06-28 (42nd Update)
- Implemented Multi-level Hand-drawn Isometric Tower Progression (Option D).
  1. Implemented L2 tall tower offset logic in drawTowerSprite: Computed dynamic drawY offset as `y - (cvs.height - 16) * scale` to support towers taller than 16px. This forces towers to grow vertically upwards while anchoring their bottom base securely on grid boundaries.
  2. Designed Level 2 custom tower assets: Created 7 unique, highly-detailed 16x24 isometric pixel art matrices for Level 2 merged towers (Fire, Water, Wood, Earth, Metal, Yin, Yang). Added thematic particle structures and light flares to the upper sections.
  3. Integrated L2 assets into TOWER_SPRITE_MAP: Registered new L2 configurations (`fire_2`, `water_2`, etc.) to trigger automatic caching and sprite loading when merging/synthesizing towers in battle.

2026-06-28 (41st Update)
- Upgraded Art Visual Hierarchy and Firing Recoil/Hit Squash Physics (Option A & B).
  1. Implemented background noise desaturation: Reduced the lightness and saturation of all map theme palettes (Sci-Fi, Chinese Wood, Ink Splatted, Starry Nebula) by 25%. This establishes a clear visual hierarchy, allowing sprites and health bars to stand out cleanly.
  2. Implemented 45-degree volumetric light shading: Upgraded all 7 enemy types (snake, fly, salamander, water_spirit, golem, beetle, boss_dragon) and all 7 element towers (Fire, Water, Wood, Earth, Metal, Yin, Yang) with high-contrast palettes, adding distinct volumetric shading and 3D silhouette pop.
  3. Added turret recoil physics simulation: Added recoilY property to towers and simulated spring-damper recoil decay (decay 0.82) when firing bullets.
  4. Added hit squash-and-stretch deformation: Added squashX/squashY properties to enemies and simulated elastic squeeze/stretch shape recovery (ratio 1.35:0.65, damper 0.15) when taking damage.
  5. Implemented parabolic pixel splatter particle system: Added gravity physics, custom drags, and square block rendering to Particle system. Triggered 3-5 square pixel splatters on hit and 8-10 on death, greatly enhancing battle juice.

2026-06-28 (40th Update)
- Implemented Retro Pixel Style Visual Polish & Procedural Map Decorators.
  1. Implemented Smart Outline shader in prerenderSprite: Automatically outlines non-transparent pixels of 16x16 cached sprites with a dark slate gray (#0f172a) stroke, making pixel art stand out pop on all maps.
  2. Added procedural Squash & Stretch animation in drawTowerSprite: Configured a time-based sine scaling with an anchor point on the tower's base center, giving retro towers a lively bouncy breathing animation.
  3. Added procedural Sway movement animation in drawEnemySprite: Introduced a rotation sway oscillation (+-7 degrees) for moving enemies, rendering cute organic walking patterns.
  4. Implemented procedural ground tile decorators: Created mossy wood (Chinese), ink splattered paper (Ink), and star studded nebula (Starry) ground tile variants. Configured cell coordinates hash to randomly distribute decorator tiles with a 20% point-density chance, breaking tile repetition.

2026-06-28 (39th Update)
- Upgraded Stable Diffusion Asset Generator Quality & Prompts.
  1. Refactored prompts in sd_batch_generate.py: Enhanced positive prefix to target 'classic 16-bit pixel art', 'flat colors', and 'clean contours'. Added dynamic perspective pre-prompts for defense towers to enforce 'isometric projection' and 'orthographic view'.
  2. Upgraded negative prompts: Added 'blurry', 'anti-aliased pixels', and 'smooth gradients' to negative tags, forcing SD to output clean, sharp-edged game sprites.
  3. Implemented smart keying edge-feathering algorithm: Rewrote remove_white_background to measure color differences, performing Alpha value linear decay for semi-transparent edges in range [200, 230] to eliminate rough white outline glows on dark background.
  4. Fixed Python script Windows compatibility: Cleaned all terminal prints to standard ASCII, avoiding CP950 UnicodeEncodeError crashes on Chinese PowerShell terminals.

2026-06-28 (38th Update)
- Upgraded Art Asset Specifications & Fixed Spritesheet Slice Misidentification.
  1. Fixed spritesheet slice misidentification in sprites.ts: Added square check. If width equals height and width is not the unit slice size (64px/96px), it's treated as a single asset. This successfully prevents existing 512x512 PNGs from being sliced incorrectly.
  2. Implemented asset optimization script (downscale_sprites.py): Wrote a Python script utilizing Pillow to batch scale all 27 existing 512x512/768x768 PNGs down to 64x64/96x96 px. Successfully optimized all files, reducing directory size by over 95%.
  3. Upgraded SD generation workflow: Modified sd_batch_generate.py to automatically downscale output image to 64px (normal) or 96px (Boss) right before saving, standardizing future assets.

2026-06-28 (37th Update)
- Implemented Multi-row Grid Spritesheet & Directional/State Animation.
  1. Implemented directional enemy animation mapping: Added vx, vy velocity vectors tracking to Enemy movement in main.ts. Spritesheet row is dynamically mapped in drawEnemySprite based on direction (Row 0=Down/Front, Row 1=Up/Back, Row 2=Left, Row 3=Right).
  2. Implemented tower state animation mapping: Enabled state-aware row selection in drawTowerSprite based on weapon firing cooldown (Row 1=Attacking, Row 0=Breathing/Idle).
  3. Structured Grid Spritesheet slicing: Unified cell slice bounds to 64px for standard units and 96px for Boss dragon. Supports multi-row grid sprite sheets dynamically.

2026-06-28 (36th Update)
- Implemented Hybrid 64x64 / 96x96 Spritesheet Animation System.
  1. Added smart aspect-ratio detection: Programmatically analyzes image dimensions of external assets (such as 64x64 or 96x96 px PNGs) inside drawEnemySprite and drawTowerSprite.
  2. Implemented frame cropping: Automatically identifies horizontal spritesheets (e.g., 128x64 or 256x64) and performs dynamic cropping (cols = width / height) for animation cycles.
  3. Integrated smooth timing cycles: Enemies loop through frames at 180ms intervals, and towers breathe at 350ms intervals, scaling assets beautifully onto the 16x16/22x22 grid.

2026-06-28 (35th Update)
- Implemented Phase 4.5: Native Pixel Art & Visual Tiling Upgrade.
  1. Upgraded enemy sprites: Unified all 7 enemy sprite matrices to 16x16 resolution with multi-frame walking/flying animations (2-4 frames). Included dynamic frame indexing based on system clock.
  2. Upgraded tower sprites: Added 2-frame subtle breathing/glowing animations to all 7 base towers.
  3. Implemented dynamic path tiling: Added path grid cache updated automatically upon tower placements/sold. Introduced 16x16 pixel floor tiles (path and ground) for 4 distinct map themes (scifi, chinese, ink, starry).
  4. Rewrote battle rendering loop: Tiled map tiles across the 80x40 canvas grid. Layered stardust and particle effects over corresponding tiles for enhanced depth.

2026-06-28 (第三十四次更新)
- 移除主選單與天賦系統大圖生成腳本與相關排版變更：
  1. 還原 index.html：移除先前新增的雙欄分欄、裝飾插圖標籤、懸浮動畫、發光層與相關 CSS 樣式，使主選單與天賦頁面恢復至原先清爽、置中的核心像素功能佈局。
  2. 清空大圖生成腳本：由於 Stable Diffusion 輸出之大圖插畫品質不佳且不符預期，已廢棄並清空專案根目錄下的 sd_generate_large.py。

2026-06-28 (第三十三次更新)
- 實裝主選單與天賦系統大圖排版與特效處理，並建立大圖生成腳本：
  1. 重構 index.html 樣式與佈局：實作主選單雙欄分界、玻璃擬態面板、懸浮動畫（@keyframes floatAnim）與背景發光特效（illustration-glow）；重構天賦頁面為左側插圖導覽與右側天賦樹佈局。
  2. 新增防禦性插圖加載：大圖標籤均綁定 onerror="this.parentElement.style.display='none'"，若大圖尚未生成或加載失敗，版面將自動優雅退回無圖排版，不影響玩家操作。
  3. 建立 sd_generate_large.py 腳本：配置專用 Prompts 與 512x512 解析度，呼叫本地 SD API 進行生成並套用白色背景高精去背，產出主選單與天賦系統大圖至 assets/sprites 目錄下。

2026-06-28 (第三十二次更新)
- 調整專案視覺設計路線並更新 ROADMAP.md：
  1. 決議棄用 SD 高畫質產圖路線：由於 512px 圖片在 16px 網格地圖下縮放後細節盡失，且 SD 生成與去背存在瑕疵，決定全面轉向「方案 D：極致原生像素風」。
  2. 更新開發路線圖：在 ROADMAP.md 中新增 Phase 4.5（極致像素風視覺升級）計劃，包含精靈規格重構至 16x16、角色動態動畫幀、戰場像素地板、像素風 HUD/UI、Canvas Bloom 後處理光效等，並於 Phase 4 標註 SD 圖片調整註記。

2026-06-28 (第三十一次更新)
- 修復美術圖片交疊與卡牌框無去背問題並新增畫質風格切換選單：
  1. 縮放與交疊優化：調整 src/sprites.ts 中防禦塔的繪製尺寸為寬 16 * scale (剛好 1 格寬，防止左右交疊) 與高 22 * scale (維持 1.3 倍高度立體感)，偏移 y - 6 * scale，使岩壁塔等連續建造時如城牆般完美拼接；普通怪物尺寸縮小至 16 * scale (1 格寬，Boss 為 26 * scale)，避免體積過大遮擋網格。
  2. 新增美術風格選單：在 index.html 頂部面板新增「👾 懷舊像素 (推薦) / 🎨 AI 高清」切換選單。於 src/main.ts 中實作全域變數 currentStyle 與 change 監聽，並將其傳入精靈渲染函式。
  3. 精靈繪製風格連動：在 src/sprites.ts 中，當前風格為 pixel 時強制繪製 100% 完美去背的內建像素精靈；為 highres 且高清圖片載入成功時才載入 SD 圖片，給予玩家在高清美術調整完成前隨時一鍵返回無瑕像素風的權利。
  4. 優化 SD 產圖提示詞：修改 sd_batch_generate.py，在正面 Prompt 增加 8-bit pixel art、no background 等詞彙；Negative Prompt 中大幅加重 card, frame, border, text, description, landscape background, sky, cloud, grass 等詞，從源頭杜絕 SD 產出卡牌邊框與藍天草地等背景。

2026-06-28 (第三十次更新)
- 實裝【測試】砲台與怪物觀賞室關卡與動態畫面放大功能：
  1. 新增測試關卡配置：在 maps.ts 導入 test_level 20x10 地圖與對應檢查點，並於 MapConfig 中擴充 difficulty 型別支援 '測試' 難度。
  2. 核心邏輯動態網格大小適配：將 COLS/ROWS/TILE_SIZE/grid 改為變數。載入測試關卡時，自動設定為 20x10 網格且每格 64 像素（放大 4 倍），完美填滿畫布。
  3. 全方位等比例渲染縮放：砲台底座/星級、怪物/血條/DOT條、子彈半徑、飄字大小、發光虛線路徑粗細、以及粒子特效初速與尺寸，皆依據網格大小比例（TILE_SIZE / 16）自動等比縮放，保持一致視覺比例。
  4. 測試關卡專屬無敵與無限規則：初始金幣設為 999999 且蓋塔不扣；怪物被打時血量不扣並顯示「免疫」字樣，保留受擊高亮與擊中粒子；怪物到基地不扣 HP 且安全消失；UI 金幣與生命顯示為「∞」；放棄結束不給予天賦點數，亦不判定勝利。
  5. 難度標籤美化：在 index.html 增加 .badge-test 粉紅色難度樣式，並於關卡選擇動態卡片綁定對應 badge。

2026-06-28 (第二十九次更新)
- 修復高品質美術去背不完全所致之正方形大圖問題：
  1. 建立診斷工具 check_sprites.py：檢查 assets/sprites 底下所有 PNG 圖片的透明通道與透明像素比例。
  2. 建立去背修復工具 fix_transparency.py：支援 Flood Fill (邊緣泛洪填充) 與 Global (全域亮色替換) 兩種高精細度去背演算法，可對現有圖片重新去背並自動覆蓋，避免主體內部白色像素被誤挖空。
  3. 清除冗餘代碼：修正 src/main.ts，移除尾部重複且載入路徑錯誤的 loadHighResAssets() 冗餘呼叫。

2026-06-28 (第二十八次更新)
- 美化 SD 高品質美術資源與實裝打擊回饋效果：
  1. 砲台底座優化：在防禦塔正下方新增繪製半透明五行屬性色的 3D 透視橢圓發光光環，改善砲台屬性難以辨識問題。
  2. 防禦塔尺寸與偏移調整：SD 砲台渲染規格由 20x20 放大至 26x26，並調整偏移 y - 10 像素，在 16x16 網格上建立偽 3D 立體高度感。
  3. 精靈圖硬邊描邊：使用 Canvas filter 硬體加速為怪物套用屬性色 1px 外框與落影；砲台套用 1px 半透明黑描邊與陰影，改善四角邊緣死板四方形感。像素 Fallback 精靈亦同享描邊美化。
  4. 怪物受擊閃爍回饋：在 Enemy 介面引入 hitFlashFrame 屬性。當子彈擊中怪物或造成範圍傷害（AOE）時，設定該怪物 hitFlashFrame 為 6 幀。在繪製時，若 hitFlashFrame > 0 則使用 filter 套用耀眼亮度對比與紅色外發光，大幅提升擊中反饋。

2026-06-28 (第二十七次更新)
- 實裝 SD 高品質美術資源導入遊戲：
  1. 修改 src/main.ts：在 initSprites() 之後加入 loadAllHighResSprites() 呼叫，批量載入全部 27 張防禦塔與怪物的高品質 SD 去背精靈圖。
  2. 與 src/sprites.ts 的 imageAssetCache 雙重渲染機制連動：當圖片載入完成後將自動無縫套用於 Canvas 畫面中，若資源尚未載入完或缺失，將安全回退至內建像素精靈渲染，達成健全防禦性資產載入。
  3. 更新 ROADMAP.md：將 Phase 4 高品質美術資源項目與 Phase 4 整體進度標記為「已完成」。
  4. 更新 docs/ART_ASSET_MANIFEST.md：將 27 張精靈圖的導入狀態更新為「🟢 已完成」。

2026-06-28 (第二十六次更新)
- Phase 4 SD 批量產圖規劃：新增美術資源管理文件與批量產圖腳本
  1. 新增 docs/ART_ASSET_MANIFEST.md：定義所有精靈圖（7 怪物 + 7 基礎塔 + 7 Lv2 塔 + 6 配方塔 = 共 27 張）的命名規範、儲存路徑、規格要求與狀態追蹤表，並附上導入後 main.ts 初始化代碼片段。
  2. 新增 docs/SD_PROMPT_GUIDE.md：記錄所有角色的完整 Stable Diffusion 提示詞設計（共通前綴、Negative Prompt、各角色專屬 Prompt），以及去背參數說明。
  3. 新增 sd_batch_generate.py：支援全量批量產圖，功能包含：
     - --force 強制重新生成（覆蓋已存在的檔案）
     - --category enemies/towers/towers_lv2/towers_recipe 只生成指定類別
     - --id <sprite_id> 只生成單一精靈圖
     - --dry-run 列出任務清單但不執行
     - 自動白底去背（threshold 可調）
     - 自動建立目錄結構 assets/sprites/{enemies,towers,towers_lv2,towers_recipe}/
     - 跳過已存在的檔案（節省時間）
  4. 修復了 sd_batch_generate.py 在 Python 3.10 以下環境（不支援 `bytes | None` 聯合型別語法）引起的 TypeError，改用 typing.Optional[bytes] 以提升舊版本相容性。

2026-06-27 (第二十五次更新)
- Refactored Tutorial Level map obstacles and added tower placement guide:
  1. Updated obstacles layout in src/maps.ts: Added a long horizontal barrier (row 15, col 0..55) and vertical guides. This forces enemies from Spawn (5,20) to walk all the way right to waypoint 3's area (x=56+) first, bypass the obstacle, and walk left to reach waypoint 1 (20,10).
  2. Implemented customized tutorial text inside refreshToolSelection() in main.ts, guiding the player to build towers at the key bottleneck coordinate (58, 17) to maximize damage efficiency across multiple pathing phases.
  3. Added a glowing dash-bordered rectangle guide for (58, 17) with "💡 推薦建造點" text helper in renderGame() during the BATTLE scene when currentMap is 'tutorial'.

2026-06-27 (第二十四次更新)
- Designed and added pathfinding unit tests in test_cli.py:
  1. Added test case for map preset obstacles (value = 2) blocking A* pathfinding.
  2. Added test cases for multi-waypoint sequence path validation (Map Editor route checking logic) under both valid and blocked layout configurations.
  3. Ran test suite successfully with all 230/230 tests passing.

2026-06-27 (第二十三次更新)
- Updated README.md documentation:
  1. Added custom map editor to the overview description.
  2. Updated scene routing map diagram with LEVEL_SELECT and MAP_EDITOR scenes.
  3. Documented maps.ts module in the project structure folder list.

2026-06-27 (第二十二次更新)
- Implemented Phase 5-2 Custom Map Editor:
  1. Extended src/maps.ts: Added loadCustomMaps, saveCustomMaps, deleteCustomMap and extended difficulty type to support '自訂'.
  2. Implemented Map Editor Scene (#mapEditorScene) in index.html: Added canvas (#editorCanvas), map name input, brush tools (📍 Spawn, 🏠 Base, 🏁 Waypoints, ⛰️ Obstacles, 🧹 Eraser), Validate, Clear, and Save buttons.
  3. Developed full editor logic in main.ts: Implemented initEditor, startEditorLoop, renderEditor, editorClickAt (brush placement), updateEditorStatus, and editorValidatePath (A* path verification).
  4. Handled multi-map integration: Merged built-in MAPS with localStorage custom maps in renderLevelSelectScreen(), with individual delete button to remove custom map records.
  5. Added '🛠️ 自建地圖' entry button on the level selection header in index.html.
  6. Updated ROADMAP.md and marked P5-2 Map Editor task as completed.

2026-06-27 (第二十一次更新)
- Implemented Phase 5 Multi-Map Selection System and Tutorial level:
  1. Created new module src/maps.ts defining layouts, difficulty tags, descriptions, starting points, base points, waypoints, and preset terrain obstacles for 4 levels (Tutorial, Easy, Hard, Medium).
  2. Implemented Level Select scene (#levelSelectScreen) in index.html with CSS grids, difficulty badges, hover transitions, and a Back button.
  3. Integrated map layouts dynamically: Wires start menu "⚔️ 開始遊戲" to "LEVEL_SELECT" scene, and dynamically generates cards from MAPS configuration.
  4. Configured Tutorial Map (W-shape corridor) with two preset U-shaped boulder wall obstacles blocking pathing to direct enemies to go in and out of checkpoints, demonstrating maximum damage efficiency.
  5. Updated main.ts: startBattle() loads spawn/base/waypoints and fills grid[x][y] = 2 for obstacles. astarFind() blocks on grid !== 0. handleSell() blocks obstacle sells with a Tip.
  6. Implemented theme-aware obstacle drawing (drawObstacle()) in renderGame(): renders glowing neon blocks in Scifi theme, brass-rimmed bricks in Chinese, ink-washed stones in Ink, and purple quartz meteorites in Starry theme.
  7. Updated ROADMAP.md and marked the Phase 5 Multi-Map Selection task as completed.

2026-06-27 (第二十次更新)
- Implemented prerequisite display on locked talent cards:
  1. In renderTalentScreen(), dynamically compute unmet prerequisites for each talent node (prerequisites where talentLevels[pid] < 1).
  2. Render a '🔒 需解鎖：[名稱]' text inside the card HTML as .talent-card-prereq when any prerequisites are unmet.
  3. Added .talent-card-prereq CSS style (orange #f97316, 0.68rem) in index.html to visually distinguish the requirement hint from cost and description text.

2026-06-27 (第十九次更新)
- Refactored Talent Screen UX to fix two layout issues:
  1. Fixed horizontal scroll empty space: Changed .talent-tree-wrapper from fixed 'max-width: 1024px' to 'max-width: 100%', and changed .talent-tree-container from 'min-width: max-content' to 'width: max-content'. This ensures the scrollbar range precisely matches the actual content width with no blank draggable space beyond content.
  2. Added Track Navigator pill-button bar: Inserted a '#talentNav' navigation bar between the talent header and the tree wrapper, containing 4 pill buttons (🛡️ 基礎強化, ⚔️ 攻擊強化, 🌿 五行覺醒, ☯️ 陰陽解鎖). Clicking a button smoothly scrolls the talent screen to the corresponding track via scrollIntoView. Added IntersectionObserver to automatically sync active highlight state with the visible track.
  3. Assigned id attributes (track-base, track-attack, track-element, track-yinyang) to all four talent-track elements for precise scroll targeting.

2026-06-27 (第十八次更新)
- Fixed the layout initialization issue where the game started stuck on the talent screen:
  1. Relocated 'display: flex' from the base '#talentScreen' ID selector rule to a new '#talentScreen.active' class rule.
  2. This prevents the high-specificity ID selector rule from overriding the default scene hiding rule '.scene { display: none }', successfully restoring proper scene transitions and the main loop entrance.

2026-06-27 (第十七次更新)
- Adjusted Talent Screen CSS constraints, changing overflow to 'overflow-y: auto' and setting 'height: 100%' to ensure vertical scrolling works on all display resolutions.
- Fixed a Flexbox scrolling bug on #talentScreen by adding 'display: flex' and 'justify-content: flex-start' to override centering alignment, resolving the vertical scroll block issue.
- Redesigned and modernized the Meta-progression Talent Screen UI:
  1. Converted the talent node layout from a generic grid into 4 horizontally extending paths (Base Upgrades, Attack Skills, Wuxing Awakenings, and Yin-Yang Secrets).
  2. The Talent Screen height is now locked to prevent vertical scrolling, allowing smooth horizontal scrolling dynamically tracking the tracks.
  3. Enlarged talent icons (emoji font-size 2.2rem) and titles, and added delicate responsive card states (glowing gold borders for 'available', green for 'unlocked', dimmed card opacity for 'locked').
  4. Implemented interactive connection paths: arrows (➜) linking prerequisite chains now dynamically light up gold when a preceding talent is unlocked.
- Added a new master Wuxing talent "Wall discount" (築牆工法):
  1. Positioned right after "Earth Awakening" in the element path, unlocking it reduces Wall (Rock wall) cost from 2g to 1g.
  2. Implemented cost deduct adjustments in main.ts placement and store definition, ensuring correct sell refunds.
  3. Added Python CLI mirroring implementations and regression assertions (all 227/227 tests passed).

2026-06-27 (第十六次更新)
- Implement map visual themes and dynamic weather overlay system for Phase 4:
  1. Added Map Themes: Chinese (Crimson & Gold imperial theme), Ink (traditional ink painting wash style on paper background), and Starry (dark starry night with twinkle background stars that scroll and scale naturally with the map camera).
  2. Added Weather Effects: Rain (falling slanted rain droplets), Fog (drifting low-opacity cloud overlays), and Thunderstorm (rain combined with lightning screen flashes and random branching electric bolts).
  3. Integrated dropdown selects in index.html battle UI header panel for seamless real-time switching between map themes and weather effects.
  4. Verified all changes via success compilation (npm run build) and python CLI regression tests.

2026-06-27 (第十五次更新)
- 建立 CLI 測試工具 `test_cli.py`：一支純 Python CLI 工具，無需瀏覽器或 DOM，鏡像 TypeScript 遊戲核心邏輯。提供三個選單入口：(1) Battle Flow Test（戰鬥主流程）、(2) Full Feature Test（全功能測試所有 8 套件）、(3) Specific Feature Test（指定套件）。包含 8 個測試套件：波次配置、A* 尋路、砲台系統、天賦系統、怪物定義、波次配置、粒子系統及 AOE 擊殺修復驗證（Bug #14）。
- 修正 `test_cli.py` 中的測試邏輯：(1) 修正 Python 3.8/3.9 舊版本的 `|` 聯集型別語法為相容寫法以防止 `TypeError`；(2) 修正 `test_battle_flow` 中的 HP 縮放測試，使其動態取得當前波次第一個怪物類型的 `baseHp`，防止因特定波次無小蛇（snake）而拋出 `StopIteration` 異常；(3) 修正 `test_aoe_kill_fix` 測試資料中火蜥蜴（salamander）的 `hp` 設定值（自 5 修改為 0），使其能如期判定為死亡以符合死亡數與金幣獲得數的斷言；(4) 修正 `test_wave_config` 中波次數量遞增測試，過濾飛行怪與 Boss 怪，改為僅統計陸地怪（ground enemies）數量以驗證陸地怪物的持續增長，避免因特定波次無飛行怪所致之總數量正常波動觸發斷言失敗。

2026-06-27 (第十四次更新)
- 修復 AOE 擊殺怪物時的 TypeError 崩潰問題：在 `src/main.ts` 中將 `enemies[j].def.colorPrimary` 改為 `ENEMY_DEFS[enemies[j].type]?.colorPrimary ?? '#facc15'`，避免在運行時讀取未定義的 `def` 屬性。

2026-06-27 (第十三次更新)
- 實作 Canvas 高性能粒子特效系統：在 `src/main.ts` 中新增粒子更新與渲染邏輯。子彈擊中怪物時噴射多彩屬性火花粒子、怪物死亡時產生能量擴散消散粒子、兩塔合成成功時爆發出紫色擴散魔幻光環，大幅增強視覺打擊感。
- 實作遊戲音效 (SFX) 播控系統：預載入 `click.mp3`、`shoot.mp3`、`enemy_death.mp3` 與 `merge_success.mp3`。透過 Audio clone 實現多聲軌重疊流暢播放，並與右上角靜音開關同步連動，提供無音效檔時的 catch 異常安全回退保護。

2026-06-27 (第十二次更新)
- 實作背景音樂三首輪播與循環播放機制：在 `src/main.ts` 中配置 `Rain_on_the_Pagoda_Roof.mp3`、`Tiles_in_Motion.mp3` 與 `Iron_River_Gate.mp3` 播放清單。每首播放完畢後會間隔 5 秒鐘自動切換播放下一首。
- 實作瀏覽器 Autoplay 解鎖機制：在玩家首次點擊畫面時自動播放音樂，並在 `index.html` 右上角新增浮動音樂開關按鈕（🎵/🔇），支援隨時靜音與重新啟動。

2026-06-27 (第十一次更新)
- 建立遊戲音訊資產目錄 `assets/audio/` 並加入說明文件，為 Phase 4 音效系統的整合預留資源存放位置。

2026-06-27 (第十次更新)
- 實作前端美術圖片載入與 Hybrid 雙重渲染機制：在 `sprites.ts` 中管理圖片載入狀態，當外置圖片 asset 存在時優先繪製高品質美術圖，否則自動 Fallback 到預設像素矩陣精靈，確保遊戲能百分之百相容且不崩潰。
- 實作行動裝置適配 (Auto-fit): 根據螢幕寬度自動 scale 遊戲容器，並新增 `@media` 樣式以移除圓角與適配。
- 實作 Canvas 手勢拖曳平移與雙指捏合縮放：新增單指拖曳平移、雙指捏合縮放，且放塔點擊座標能自動反向計算，確保拖曳/縮放後下塔位置完美精確。
- 在專案根目錄中建立了 `test_sd_api.py`，用於檢驗 Stable Diffusion API 及示範自動去背處理。

2026-06-27 (第九次更新)
- 更新開發路線圖 `ROADMAP.md`：經檢查，Phase 3 的「合成塔實裝（包括 AOE, DOT, 減速, 增益等邏輯）」、「售塔功能（包含右鍵快捷售塔）」、「波次進度列（UI與怪物數量連動）」以及「結算統計面板（連殺與輸出傷害統計）」之核心 TypeScript 邏輯已全數於先前實作完畢。本次更新正式將 Phase 3 標記為已完成（✅），並更新文件日期。

2026-06-27 (第八次更新)
- 補全與優化 `.gitignore` 配置：新增常見編輯器快取與設定檔（如 .vscode, .idea, .DS_Store, Thumbs.db）、日誌檔案、環境變數檔案（.env*）等，確保在多平台開發時不會將無關與敏感檔案推送到 GitHub 倉庫。

2026-06-26 (第七次更新)
- 修正 `opencode.json` 的格式：將原先不符合官方 Schema 的 `customCommands` 陣列修改為符合規格的 `command` 物件格式，並加入 `$schema` 以支援 IDE 自動補全與驗證。
- 修改 `verify_opencode.py` 驗證腳本：調整第 3 步驟的驗證邏輯，使其能正確讀取並比對新的 `command` 物件結構中的 `template` 欄位與 `package.json` scripts。

2026-06-26 (第六次更新)
- 新增 `verify_opencode.py` 檢測腳本，用於驗證 `opencode.json` 的 JSON 格式與自訂指令的對應性，並測試 `package.json` 中的 scripts 及 TypeScript 編譯狀態。

2026-06-26 (第五次更新)
- 撰寫專案 README.md：完整記錄遊戲簡介、核心玩法、砲台體系、怪物陣容、天賦系統、技術規格、本地開發指引與專案結構
- 撰寫專案 ROADMAP.md：涵蓋 Phase 0~6，記錄已完成里程碑與未來計劃方向（內容完善、視覺升級與適配、遊戲深度擴展、探索性功能、技術債項目）
- 因應手機版需求，於 Phase 4 新增「行動裝置畫面適配與縮放」規劃（支援 Auto-fit 縮放、+/- 縮放按鈕、拖曳平移與固定 UI 面板）

2026-06-26 (第四次更新)
- 合成機制優化：移除「必須相鄰」的合成位置限制，現在玩家可以任意選取地圖上的兩座砲台進行合成；點擊選取同一座塔則會安全取消選取
- 系統提示優化：將合成成功、合成失敗、不能堵死怪物、已有建物、金幣不足、防禦成功等關鍵系統反饋的飄字大小放大至 15px 或 16px，提升視覺反饋清晰度
- 更新 GDD 說明文件

2026-06-26 (第三次更新)
- 調整防禦塔設定：岩壁塔造價從 8g 降低至 2g 作為基礎建設，烈焰塔調整為預設解鎖之第一款攻擊型武器，提升前期體驗
- 重構天賦系統為多級升級：天賦樹中的所有品項均支援多次升級強化，UI 介面增加等級進度顯示（例如 Lv.1/5），並調整天賦儲存結構與舊資料相容邏輯
- 新增路線預覽功能：在戰鬥畫面下方新增「路線預覽」按鈕，點擊後會以 A* 計算並渲染陸地怪物的預期行走軌跡，以半透明天藍色帶流動感的虛線在畫面上顯示 3 秒後自動漸隱消失
- 更新 GDD 說明文檔

2026-06-26 (第二次更新)
- 重大系統擴展：新增主介面場景管理（MainMenu / Talent / Battle / GameOver 四場景切換）
- 新增天賦系統 (Meta-Progression)：失敗或通關都獲得天賦點，用於解鎖五行砲台與強化基礎數值，數據以 localStorage 持久化
- 砲台系統重構為五行（金木水火土）+ 陰陽（陰陽）七種基礎砲台，含同系 Lv2 合成與異系配方合成（五行相生 + 陰陽太極）
- 怪物系統擴展為六種常規怪 + Boss，帶有五行屬性標記，與砲台形成相剋加成關係
- 像素美術系統：所有怪物（小蛇、蒼蠅、火蜥蜴、水靈、石傀儡、金甲蟲、龍影）與砲台均以程式碼內建像素矩陣繪製
- 新增模組檔案：towers.ts, enemies.ts, talent.ts, sprites.ts
- GDD 文檔全面更新

2026-06-26
- 初始化專案調研與規劃，產出遊戲設計提案與專案路線圖檔案。
- 建立專案文件記錄規範與模板檔案，包含 AI 協同開發指南 (AI_Coding_Guidelines.md) 與遊戲設計文檔模板 (GDD_Template.md)。
